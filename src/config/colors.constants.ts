/**
 * Theme token aliases for icon picker states.
 * Values reference preset b2oDq0a9a CSS variables in src/index.css — do not hardcode hex here.
 */

/** Included in registry / lime accent — `--primary` in Lime theme. */
export const SELECTED_GREEN = 'var(--primary)' as const;

/** Focused icon in grid — `--ring` (theme focus token). */
export const ACTIVE_BLUE = 'var(--ring)' as const;
